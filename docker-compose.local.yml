services:
  app:
    environment:
      MONGO_URI: mongodb://mongo:27017/realestate_crm
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    container_name: realestate-crm-mongo
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
