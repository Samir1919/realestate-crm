<div class="lg:hidden space-y-3 min-w-0">
    <% leads.forEach((lead)=> { %>
        <%- include('mobile-card', { lead }) %>
            <% }); %>

                <% if (leads.length===0) { %>
                    <div class="text-center py-8 text-slate-400 bg-white border border-slate-200 rounded-2xl shadow-sm">
                        No leads found.
                    </div>
                    <% } %>
</div>