<%- include('partials/header') %>
    <%- include('partials/sidebar') %>

        <div class="w-full min-w-0 overflow-x-hidden">
            <div class="safe-container mx-auto w-full max-w-[1600px] space-y-6 px-4 py-6 sm:px-6 lg:px-8">
                <%- include('partials/ui/page-header', { title: 'CRM Analytics Overview' ,
                    subtitle: 'Quick snapshot of your current pipeline.' , action: { type: 'link' , href: '/logout' ,
                    label: 'Logout' ,
                    className: 'crm-btn-danger inline-flex items-center rounded-xl px-3 py-2 text-sm font-semibold shadow-sm'
                    } }) %>

                    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 xl:grid-cols-3">
                        <div class="crm-card-soft flex items-center justify-between p-6">
                            <div>
                                <p class="text-sm font-semibold uppercase tracking-wider text-slate-500">Total Leads</p>
                                <h3 class="mt-1 text-4xl font-bold text-slate-900">
                                    <%= leads.length %>
                                </h3>
                            </div>
                            <div class="rounded-xl bg-blue-50 p-3 text-xl text-blue-600">
                                <i class="fa-solid fa-user-tag"></i>
                            </div>
                        </div>
                    </div>
            </div>
        </div>



        <%- include('partials/footer') %>