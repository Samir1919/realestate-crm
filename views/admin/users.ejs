<%- include('../partials/header') %>
    <%- include('../partials/sidebar') %>

        <div class="w-full min-w-0 overflow-x-hidden">
            <div class="safe-container mx-auto w-full px-4 sm:px-6 lg:px-8 py-6 space-y-6 max-w-[1600px]">
                <%- include('../partials/ui/page-header', { title: 'User Management' ,
                    subtitle: 'Assign role access for your team' , action: { type: 'button' , id: 'openAddUserModal' ,
                    label: 'Add User' ,
                    className: 'crm-btn-primary inline-flex items-center rounded-xl px-3 py-2 text-sm font-semibold text-white shadow-sm'
                    } }) %>

                    <% if (created) { %>
                        <%- include('../partials/ui/flash-message', { variant: 'success' ,
                            message: 'New user created successfully with default role: viewer.' }) %>
                            <% } %>

                                <% if (error) { %>
                                    <%- include('../partials/ui/flash-message', { variant: 'error' , message: error })
                                        %>
                                        <% } %>

                                            <div class="grid grid-cols-1 gap-2.5 lg:hidden sm:grid-cols-2 sm:gap-3">
                                                <% users.forEach((item)=> { %>
                                                    <div class="crm-card rounded-2xl p-3 sm:p-3.5">
                                                        <div class="flex items-start justify-between gap-3">
                                                            <div class="min-w-0">
                                                                <p
                                                                    class="safe-truncate text-sm font-semibold text-slate-900 sm:text-[15px]">
                                                                    <%= item.name %>
                                                                </p>
                                                                <p
                                                                    class="mt-0.5 safe-truncate text-[11px] text-slate-600 sm:text-xs">
                                                                    <%= item.email %>
                                                                </p>
                                                            </div>
                                                            <span
                                                                class="inline-flex rounded-full border px-2 py-0.5 text-xs font-medium capitalize <%= item.role === 'admin' ? 'border-violet-200 bg-violet-50 text-violet-700' : item.role === 'sales' ? 'border-amber-200 bg-amber-50 text-amber-700' : 'border-sky-200 bg-sky-50 text-sky-700' %>">
                                                                <%= item.role %>
                                                            </span>
                                                        </div>

                                                        <form method="POST" action="/admin/users/<%= item._id %>/role"
                                                            class="mt-2 flex items-center gap-1.5 sm:mt-2.5">
                                                            <select name="role"
                                                                class="w-full rounded-xl border border-slate-300 bg-white px-2 py-1.5 text-sm text-slate-700">
                                                                <% roles.forEach((role)=> { %>
                                                                    <option value="<%= role.name %>"
                                                                        <%=item.role===role.name ? 'selected' : '' %>>
                                                                        <%= role.name %>
                                                                    </option>
                                                                    <% }) %>
                                                            </select>
                                                            <button type="submit"
                                                                class="crm-btn-primary shrink-0 rounded-xl px-2 py-1.5 text-sm font-semibold text-white">Update</button>
                                                        </form>
                                                    </div>
                                                    <% }) %>

                                                        <% if (users.length===0) { %>
                                                            <div
                                                                class="rounded-2xl border border-slate-200 bg-white p-6 text-center text-slate-400 sm:col-span-2">
                                                                No users found.
                                                            </div>
                                                            <% } %>
                                            </div>

                                            <div class="crm-table-shell hidden w-full min-w-0 overflow-hidden lg:block">
                                                <div class="w-full min-w-0 overflow-x-auto">
                                                    <table class="w-full min-w-[720px] text-sm">
                                                        <thead>
                                                            <tr
                                                                class="bg-slate-50 text-slate-500 text-xs uppercase tracking-wide">
                                                                <th class="text-left p-3 font-semibold">Name</th>
                                                                <th class="text-left p-3 font-semibold">Email</th>
                                                                <th class="text-left p-3 font-semibold">Current Role
                                                                </th>
                                                                <th class="text-left p-3 font-semibold">Assign Role</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% users.forEach((item)=> { %>
                                                                <tr
                                                                    class="border-t border-slate-100 hover:bg-slate-50/80 transition-colors">
                                                                    <td class="p-3"><span
                                                                            class="font-medium text-slate-900">
                                                                            <%= item.name %>
                                                                        </span></td>
                                                                    <td class="p-3 text-slate-600">
                                                                        <%= item.email %>
                                                                    </td>
                                                                    <td class="p-3">
                                                                        <span
                                                                            class="inline-flex rounded-full border px-2 py-0.5 text-xs font-medium capitalize <%= item.role === 'admin' ? 'border-violet-200 bg-violet-50 text-violet-700' : item.role === 'sales' ? 'border-amber-200 bg-amber-50 text-amber-700' : 'border-sky-200 bg-sky-50 text-sky-700' %>">
                                                                            <%= item.role %>
                                                                        </span>
                                                                    </td>
                                                                    <td class="p-3">
                                                                        <form method="POST"
                                                                            action="/admin/users/<%= item._id %>/role"
                                                                            class="flex items-center gap-2">
                                                                            <select name="role"
                                                                                class="rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-700">
                                                                                <% roles.forEach((role)=> { %>
                                                                                    <option value="<%= role.name %>"
                                                                                        <%=item.role===role.name
                                                                                        ? 'selected' : '' %>>
                                                                                        <%= role.name %>
                                                                                    </option>
                                                                                    <% }) %>
                                                                            </select>
                                                                            <button type="submit"
                                                                                class="crm-btn-primary rounded-xl px-3 py-2 text-sm font-semibold text-white">Update</button>
                                                                        </form>
                                                                    </td>
                                                                </tr>
                                                                <% }) %>
                                                                    <% if (users.length===0) { %>
                                                                        <tr>
                                                                            <td colspan="4"
                                                                                class="p-8 text-center text-slate-400">
                                                                                No users found.
                                                                            </td>
                                                                        </tr>
                                                                        <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                            </div>
            </div>
        </div>

        <div id="addUserModal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50 p-4">
            <div class="w-full max-w-lg rounded-2xl border border-slate-200 bg-white shadow-2xl">
                <div class="flex items-center justify-between border-b border-slate-200 px-5 py-4">
                    <h2 class="text-lg font-semibold text-slate-900">Add New User</h2>
                    <button type="button" id="closeAddUserModal"
                        class="rounded-lg px-2 py-1 text-slate-500 hover:bg-slate-100 hover:text-slate-700">&times;</button>
                </div>
                <form method="POST" action="/admin/users/create" class="space-y-4 p-5">
                    <div>
                        <label class="mb-1 block text-sm font-medium text-slate-700">Name</label>
                        <input name="name" type="text" required
                            class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-100" />
                    </div>
                    <div>
                        <label class="mb-1 block text-sm font-medium text-slate-700">Email</label>
                        <input name="email" type="email" required
                            class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-100" />
                    </div>
                    <div>
                        <label class="mb-1 block text-sm font-medium text-slate-700">Password</label>
                        <input name="password" type="password" required
                            class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-100" />
                    </div>
                    <p class="rounded-xl border border-sky-200 bg-sky-50 px-3 py-2 text-sm text-sky-700">New users are
                        created as viewer by default. Role can be updated from this page.</p>
                    <div class="flex items-center justify-end gap-2 pt-2">
                        <button type="button" id="cancelAddUserModal"
                            class="crm-btn-secondary rounded-xl px-3 py-2 text-sm font-semibold text-slate-700">Cancel</button>
                        <button type="submit"
                            class="crm-btn-primary rounded-xl px-3 py-2 text-sm font-semibold text-white">Create
                            User</button>
                    </div>
                </form>
            </div>
        </div>

        <script>
            const addUserModal = document.getElementById('addUserModal');
            const openAddUserModal = document.getElementById('openAddUserModal');
            const closeAddUserModal = document.getElementById('closeAddUserModal');
            const cancelAddUserModal = document.getElementById('cancelAddUserModal');

            function showAddUserModal() {
                addUserModal.classList.remove('hidden');
                addUserModal.classList.add('flex');
                document.body.style.overflow = 'hidden';
            }

            function hideAddUserModal() {
                addUserModal.classList.add('hidden');
                addUserModal.classList.remove('flex');
                document.body.style.overflow = '';
            }

            if (addUserModal && openAddUserModal && closeAddUserModal && cancelAddUserModal) {
                openAddUserModal.addEventListener('click', showAddUserModal);
                closeAddUserModal.addEventListener('click', hideAddUserModal);
                cancelAddUserModal.addEventListener('click', hideAddUserModal);

                addUserModal.addEventListener('click', (event) => {
                    if (event.target === addUserModal) {
                        hideAddUserModal();
                    }
                });
            }
        </script>

        <%- include('../partials/footer') %>